import MainInfo from "../layouts/MainInfo.jsx"
import Nav from "../layouts/Nav.jsx"

const Header = () => {
  return (
    <header>
        <Nav/>
        <MainInfo/>
    </header>
  )
}

export default Header